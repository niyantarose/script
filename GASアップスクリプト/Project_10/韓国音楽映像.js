/**
 * 韓国音楽映像.gs
 * 韓国音楽映像シート専用の設定とメニューラッパー
 */

const 設定_韓国音楽映像 = {
  マスターシート名: '韓国音楽映像',
  作品シート名: 'Works（韓国音楽映像）',
  言語マスター名: '言語マスター',
  カテゴリマスター名: 'カテゴリマスター',
  形態マスター名: '形態マスターシート',

  作品ヘッダー: ['WorksKey', '作品ID', '日本語タイトル', 'アーティスト', '原題タイトル', '登録済み数', '最新数', '更新日時', '最新数(予約込み)', '予約更新日時'],
  作品列数: 10,

  色パレット: ['#b7e1cd', '#fce8b2', '#f4c7c3', '#c9daf8', '#d9d2e9', '#fce5cd', '#fff2cc', '#d9ead3', '#cfe2f3', '#f4cccc'],

  監視列: [
    'アーティスト名', '商品名(日本語)', '商品名(原題)',
    '言語', 'カテゴリ', '特典メモ',
    'JANコード'
  ],

  列名: {
    発行チェック:     '発番発行',
    商品コード:       '管理コード',
    タイトル:         'タイトル(自動)',
    作者:             'アーティスト名',
    日本語タイトル:   '商品名(日本語)',
    原題:             '商品名(原題)',
    形態:             '',        // ← 空にする（形態列なし）
    言語:             '言語',
    カテゴリ:         'カテゴリ',
    単巻数:           '',
    セット開始:       '',
    セット終了:       '',
    特典メモ:         '特典メモ',
    ISBN:             'JANコード',
    作品ID:           '作品番号',
    SKU自動:          '管理コード',
    コードステータス: 'コードステータス',
    登録状況:         '登録状況'
  }
};

/* ============================================================
 * 韓国音楽映像 メニューラッパー
 * ============================================================ */
function 韓国音楽映像_確定発行()       { 確定発行を実行(設定_韓国音楽映像); }
function 韓国音楽映像_削除()           { 削除を実行(設定_韓国音楽映像); }
function 韓国音楽映像_一括更新()       { 一括更新を実行(設定_韓国音楽映像); }
function 韓国音楽映像_プルダウン更新() { プルダウン更新を実行(設定_韓国音楽映像); }

/* ============================================================
 * 韓国音楽映像シート作成
 * ============================================================ */
function 韓国音楽映像シートを作成() {
  const ui = SpreadsheetApp.getUi();
  const ss = SpreadsheetApp.getActive();
  const シート名 = '韓国音楽映像';
  if (ss.getSheetByName(シート名)) { ui.alert(`「${シート名}」シートは既に存在します`); return; }

  const sh = ss.insertSheet(シート名);
   const ヘッダー = [
    '登録状況', '管理コード', 'タイトル(自動)', '作品番号', '言語コード', 'カテゴリコード',
    '商品名(日本語)', '商品名(原題)', '言語', 'カテゴリ', '売価', '原価', '特典メモ', '配送パターン', '登録者', '備考',
    'アーティスト名', '事務所/レーベル', '発売日', 'JANコード',
    'アラジン商品コード', 'アラジンURL',
    'yes24商品コード', 'yes24URL',
    'Kyobo商品コード', 'KyoboURL',
    'メイン画像URL', '追加画像URL',
    '重複チェック用正規化タイトル', '登録日'
  ];

  sh.getRange(1, 1, 1, ヘッダー.length).setValues([ヘッダー]);

  const 自動列 = ['管理コード', 'タイトル(自動)', '作品番号', '言語コード', 'カテゴリコード', '重複チェック用正規化タイトル', '登録日'];
  const API列 = ['商品名(原題)', 'アーティスト名', '事務所/レーベル', '発売日', 'JANコード', '原価',
                 'アラジン商品コード', 'アラジンURL', 'yes24商品コード', 'yes24URL',
                 'Kyobo商品コード', 'KyoboURL', 'メイン画像URL', '追加画像URL'];

  for (let i = 0; i < ヘッダー.length; i++) {
    const cell = sh.getRange(1, i + 1);
    let 色 = '#4a86e8';
    if (自動列.includes(ヘッダー[i])) 色 = '#999999';
    if (API列.includes(ヘッダー[i])) 色 = '#e69138';
    cell.setBackground(色);
    cell.setFontColor('#ffffff');
    cell.setFontWeight('bold');
    cell.setHorizontalAlignment('center');
  }

  sh.setRowHeight(1, 40);
  sh.setFrozenRows(1);
  sh.setFrozenColumns(2);

  const 最終行 = 1000;
  sh.getRange(2, 1, 最終行 - 1, 1).setDataValidation(
    SpreadsheetApp.newDataValidation().requireValueInList(['未登録', '登録済み'], true).build()
  );

  const 全体範囲 = sh.getRange(2, 1, 最終行 - 1, ヘッダー.length);
  sh.setConditionalFormatRules([
    SpreadsheetApp.newConditionalFormatRule()
      .whenFormulaSatisfied('=$A2="未登録"')
      .setBackground('#ffff00')
      .setRanges([全体範囲])
      .build()
  ]);

  ui.alert('✅ 韓国音楽映像シートを作成しました\n\n韓国音楽映像シートをアクティブにした状態で\nメニュー「⑥プルダウン更新」を実行してください');
}