# 実運用手順

このディレクトリ（`画像分割カットツール/`）直下にある `起動.bat` から起動してください。

## 手順

1. `画像分割カットツール/起動.bat` をダブルクリック
2. 初回起動時は `desktop_tool/run.bat` が仮想環境作成と依存導入を実行
3. GUI起動後に画像分割・翻訳を実施

## 構成

- `起動.bat`: ルート起動
- `desktop_tool/`: 実体アプリ
- `desktop_tool/settings.json`: APIキー保存先（起動後に生成）
