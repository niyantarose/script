# 台湾CN → Yahoo商品登録テンプレ 送信ボタン Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `商品登録よろしく_台湾CN` の各タブでA列チェックON行を、ボタン1つで別ブック `Yahoo商品登録テンプレ` の「①商品入力シート」に追記する。

**Architecture:** Project_01（ソースブックにバインドされたGAS）に新規ファイル `Yahooテンプレ送信.js` を追加。各タブのヘッダー行を**実行時に読み**、候補名リストで列を解決（タブ差を吸収）。送信先は `SpreadsheetApp.openById` で開き、ヘッダー名で書込列を特定して最終データ行の下に追記。重複は商品コードでスキップ、送信後にチェックOFF。既存の `Project_19/【大邱】データ転送.js` の作法（ロック・確認ダイアログ・トースト）を踏襲。

**Tech Stack:** Google Apps Script (V8), clasp（Project_01 へ push）, Node.js（純粋ヘルパーの単体テスト）

## Global Constraints

- ソース spreadsheetId: `1OSoZnNTMHrH5YgU-j7zwOsyQsjS6bcwjfZxc3kzeKWM`（Project_01 バインド／active）
- 送信先 spreadsheetId: `1jN-3aLsLg_wRjLQU-mDqaImVVBgdpvwJWsHegxSJRF0`
- 送信先タブ名: `①商品入力シート`（ヘッダー1〜2行 / データ3行目〜 / 最終データ行の下に追記）
- 横断する台湾系タブ: `台湾まんが` / `台湾書籍その他` / `台湾グッズ` / `台湾雑誌`
- 行選択: A列チェックボックス = TRUE
- 列マッピング（Yahoo ← ソース候補名）:
  - 商品名 ← `タイトル` / `商品名（出品用）` / `雑誌名`
  - 商品コード ← `親コード` / `商品コード（SKU）`
  - 発売日 ← `発売日`
  - JANコード ← `ISBN`
  - 配送グループ管理番号 ← `配送パターン`（値そのまま）
- 重複: 送信先「商品コード」に既存なら追記しない。商品コード空の行はスキップ。
- 送信後: 成功行のA列チェックを FALSE に。
- 関数名: `台湾_Yahooテンプレへ送信`（図形ボタンに割当）/ 構造確認 `台湾_Yahoo送信_構造を確認`
- 純粋ヘルパーは `globalThis` に載せ、Nodeの `vm` で読めるようにする（テスト可能化）。GAS実行時は通常のグローバル関数として動く。

---

### Task 1: 設定とヘッダー解決の純粋ヘルパー（Nodeテスト）

**Files:**
- Create: `GASアップスクリプト/Project_01/Yahooテンプレ送信.js`
- Create: `GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js`

**Interfaces:**
- Produces:
  - `YAHOO_TRANSFER_CFG`（object）: `{ destId, destSheet, destHeaderRows, destDataStartRow, srcSheets:string[], fieldCandidates:{[yahooField]:string[]} }`
  - `yt_ヘッダー名マップ_(headerRows: string[][]): {[name:string]: number}` — 複数ヘッダー行を統合し、正規化したヘッダー名→0始まり列indexのmap。後勝ちしない（最初に出た列を採用）。
  - `yt_列を解決_(headerMap: {[name]:number}, candidates: string[]): number` — 候補名のうち最初に見つかった列index。無ければ -1。
  - `yt_ヘッダー正規化_(s): string` — 全角半角・空白・括弧ゆれを吸収するキー化。

- [ ] **Step 1: 失敗するテストを書く**

`GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js`:

```javascript
const fs = require('fs');
const path = require('path');
const vm = require('vm');
const ctx = { console };
ctx.globalThis = ctx;
// SpreadsheetApp 等のGAS APIはダミー（純粋ヘルパーのみ検証するため未使用）
ctx.SpreadsheetApp = {};
ctx.LockService = {};
ctx.Logger = { log: () => {} };
vm.createContext(ctx);
vm.runInContext(
  fs.readFileSync(path.join(__dirname, '..', 'Yahooテンプレ送信.js'), 'utf8'),
  ctx,
  { filename: 'Yahooテンプレ送信.js' }
);

let failed = 0;
function eq(name, actual, expected) {
  const a = JSON.stringify(actual), e = JSON.stringify(expected);
  if (a !== e) { failed++; console.error(`[NG] ${name}: expected=${e} actual=${a}`); }
  else { console.log(`[OK] ${name}`); }
}

// ヘッダー2行を統合し、各列名→indexを解決できる
const headerRows = [
  ['発番発行', '登録状況', '商品コードステータス', '親コード', 'タイトル', '作者', '', 'リンク', '', '', '', '', '', '', '', '', '', '', '', '', '配送パターン', '', 'ISBN', '発売日'],
  [],
];
const map = ctx.yt_ヘッダー名マップ_(headerRows);
eq('タイトル列', ctx.yt_列を解決_(map, ['タイトル', '商品名（出品用）', '雑誌名']), 4);
eq('商品コード列(親コード優先)', ctx.yt_列を解決_(map, ['親コード', '商品コード（SKU）']), 3);
eq('発売日列', ctx.yt_列を解決_(map, ['発売日']), 23);
eq('ISBN列', ctx.yt_列を解決_(map, ['ISBN']), 22);
eq('配送パターン列', ctx.yt_列を解決_(map, ['配送パターン']), 20);
eq('無い候補は-1', ctx.yt_列を解決_(map, ['存在しない列']), -1);

// 括弧/空白ゆれを吸収（全角括弧・前後空白）
const map2 = ctx.yt_ヘッダー名マップ_([['  商品コード（ＳＫＵ） ']]);
eq('括弧空白ゆれ吸収', ctx.yt_列を解決_(map2, ['商品コード（SKU）']), 0);

process.exit(failed ? 1 : 0);
```

- [ ] **Step 2: テスト実行（失敗確認）**

Run: `node "GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js"`
Expected: FAIL（`yt_ヘッダー名マップ_ is not a function`）

- [ ] **Step 3: 最小実装**

`GASアップスクリプト/Project_01/Yahooテンプレ送信.js`（先頭に追加）:

```javascript
// ============================================================
// 台湾CN → Yahoo商品登録テンプレ 送信
// ============================================================

var YAHOO_TRANSFER_CFG = {
  destId: '1jN-3aLsLg_wRjLQU-mDqaImVVBgdpvwJWsHegxSJRF0',
  destSheet: '①商品入力シート',
  destHeaderRows: 2,        // 1〜2行目がヘッダー
  destDataStartRow: 3,      // データは3行目から
  srcSheets: ['台湾まんが', '台湾書籍その他', '台湾グッズ', '台湾雑誌'],
  checkboxCol: 1,           // A列
  // Yahoo列名 ← ソース候補名（左優先）
  fieldCandidates: {
    '商品名': ['タイトル', '商品名（出品用）', '雑誌名'],
    '商品コード': ['親コード', '商品コード（SKU）'],
    '発売日': ['発売日'],
    'JANコード': ['ISBN'],
    '配送グループ管理番号': ['配送パターン'],
  },
};

function yt_ヘッダー正規化_(s) {
  return String(s == null ? '' : s)
    .normalize('NFKC')               // 全角英数/括弧などを半角化
    .replace(/[\s　]+/g, '')      // 空白除去
    .trim();
}

function yt_ヘッダー名マップ_(headerRows) {
  var map = {};
  for (var r = 0; r < headerRows.length; r++) {
    var row = headerRows[r] || [];
    for (var c = 0; c < row.length; c++) {
      var key = yt_ヘッダー正規化_(row[c]);
      if (!key) continue;
      if (!(key in map)) map[key] = c;  // 最初に出た列を採用
    }
  }
  return map;
}

function yt_列を解決_(headerMap, candidates) {
  for (var i = 0; i < (candidates || []).length; i++) {
    var key = yt_ヘッダー正規化_(candidates[i]);
    if (key in headerMap) return headerMap[key];
  }
  return -1;
}
```

- [ ] **Step 4: テスト実行（成功確認）**

Run: `node "GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js"`
Expected: PASS（全 `[OK]`、exit 0）

- [ ] **Step 5: コミット**

```bash
git add "GASアップスクリプト/Project_01/Yahooテンプレ送信.js" "GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js"
git commit -m "feat(yahoo-transfer): 設定とヘッダー解決ヘルパー（Nodeテスト付き）"
```

---

### Task 2: 行マッピングと重複キーの純粋ヘルパー（Nodeテスト）

**Files:**
- Modify: `GASアップスクリプト/Project_01/Yahooテンプレ送信.js`
- Modify: `GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js`

**Interfaces:**
- Consumes: `YAHOO_TRANSFER_CFG`, `yt_列を解決_`, `yt_ヘッダー名マップ_`
- Produces:
  - `yt_商品コードキー_(v): string` — 商品コードの照合キー（NFKC・空白除去・ハイフン統一・大文字化）。重複判定と空判定に使う。
  - `yt_行から送信値_(rowValues: any[], headerMap): {[yahooField]: any}` — 1行を Yahoo各列の値オブジェクトにマッピング。該当列が無い項目は `''`。

- [ ] **Step 1: 失敗するテストを書く**（既存テストの `process.exit` 直前に追記）

```javascript
// 商品コードキー: NFKC・空白除去・ハイフン統一・大文字化
eq('コードキー全角', ctx.yt_商品コードキー_('ＴＷＳ０１５２-ＣＭ-０４'), 'TWS0152-CM-04');
eq('コードキー空白/ダッシュ', ctx.yt_商品コードキー_(' tws0152‐cm‐04 '), 'TWS0152-CM-04');
eq('コードキー空', ctx.yt_商品コードキー_('  '), '');

// 行マッピング: ヘッダーに対応する列の値を取る。無い項目は空。
const mapM = ctx.yt_ヘッダー名マップ_([
  ['発番発行','登録状況','商品コードステータス','親コード','タイトル','作者','','','','','','','','','','','','','','','配送パターン','','ISBN','発売日'],
]);
const row = ['', true, '生成済み', 'TWS0152-CM-04', '台湾版まんが(特装版)『…』', '原作:…', '','','','','','','','','','','','','','','佐川','','9784000000000','2026/06/18'];
const mapped = ctx.yt_行から送信値_(row, mapM);
eq('商品名', mapped['商品名'], '台湾版まんが(特装版)『…』');
eq('商品コード', mapped['商品コード'], 'TWS0152-CM-04');
eq('発売日', mapped['発売日'], '2026/06/18');
eq('JANコード', mapped['JANコード'], '9784000000000');
eq('配送グループ管理番号', mapped['配送グループ管理番号'], '佐川');
```

- [ ] **Step 2: テスト実行（失敗確認）**

Run: `node "GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js"`
Expected: FAIL（`yt_商品コードキー_ is not a function`）

- [ ] **Step 3: 最小実装**（`Yahooテンプレ送信.js` の純粋ヘルパー群に追記）

```javascript
function yt_商品コードキー_(v) {
  return String(v == null ? '' : v)
    .normalize('NFKC')
    .replace(/[‐‑‒–—―−ー]/g, '-')   // 各種ダッシュ/長音をハイフンに
    .replace(/[\s　]+/g, '')
    .toUpperCase()
    .trim();
}

function yt_行から送信値_(rowValues, headerMap) {
  var out = {};
  var fields = YAHOO_TRANSFER_CFG.fieldCandidates;
  Object.keys(fields).forEach(function (yahooField) {
    var col = yt_列を解決_(headerMap, fields[yahooField]);
    out[yahooField] = (col >= 0 && col < rowValues.length) ? rowValues[col] : '';
  });
  return out;
}
```

- [ ] **Step 4: テスト実行（成功確認）**

Run: `node "GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js"`
Expected: PASS（全 `[OK]`、exit 0）

- [ ] **Step 5: コミット**

```bash
git add "GASアップスクリプト/Project_01/Yahooテンプレ送信.js" "GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js"
git commit -m "feat(yahoo-transfer): 行マッピングと商品コードキーのヘルパー"
```

---

### Task 3: 構造確認関数（実シートのヘッダーをログ出力）

GAS実行が必要。SpreadsheetApp依存のためNodeテストではなく、GASエディタで実行しLoggerで確認する（既存 `大邱_セルフテスト` と同様の手動検証）。

**Files:**
- Modify: `GASアップスクリプト/Project_01/Yahooテンプレ送信.js`

**Interfaces:**
- Consumes: `YAHOO_TRANSFER_CFG`, `yt_ヘッダー名マップ_`, `yt_列を解決_`
- Produces: `台湾_Yahoo送信_構造を確認()` — 各ソースタブと送信先の検出列をLoggerに出す（候補名が実ヘッダーに当たっているか確認用）

- [ ] **Step 1: 実装**

```javascript
function 台湾_Yahoo送信_構造を確認() {
  var ss = SpreadsheetApp.getActive();
  var cfg = YAHOO_TRANSFER_CFG;
  var lines = [];

  cfg.srcSheets.forEach(function (name) {
    var sh = ss.getSheetByName(name);
    if (!sh) { lines.push('[ソース欠落] ' + name); return; }
    var headerRows = sh.getRange(1, 1, 1, sh.getLastColumn()).getDisplayValues();
    var map = yt_ヘッダー名マップ_(headerRows);
    var detail = Object.keys(cfg.fieldCandidates).map(function (f) {
      var c = yt_列を解決_(map, cfg.fieldCandidates[f]);
      return f + '=' + (c >= 0 ? '列' + (c + 1) : '×なし');
    }).join(' / ');
    lines.push('[ソース] ' + name + ' : ' + detail);
  });

  var dss = SpreadsheetApp.openById(cfg.destId);
  var dsh = dss.getSheetByName(cfg.destSheet);
  if (!dsh) {
    lines.push('[送信先欠落] ' + cfg.destSheet);
  } else {
    var dHeader = dsh.getRange(1, 1, cfg.destHeaderRows, dsh.getLastColumn()).getDisplayValues();
    var dMap = yt_ヘッダー名マップ_(dHeader);
    var dDetail = Object.keys(cfg.fieldCandidates).map(function (f) {
      var c = yt_列を解決_(dMap, [f]);
      return f + '=' + (c >= 0 ? '列' + (c + 1) : '×なし');
    }).join(' / ');
    lines.push('[送信先] ' + cfg.destSheet + ' : ' + dDetail);
  }

  Logger.log(lines.join('\n'));
  SpreadsheetApp.getActive().toast('構造確認をログ出力しました（実行ログを確認）');
}
```

- [ ] **Step 2: コミット**

```bash
git add "GASアップスクリプト/Project_01/Yahooテンプレ送信.js"
git commit -m "feat(yahoo-transfer): 構造確認関数（実ヘッダーをログ出力）"
```

- [ ] **Step 3: 手動検証（ユーザー実行）**

`clasp push`（Task 6で実施）後、GASエディタで `台湾_Yahoo送信_構造を確認` を実行 → 実行ログで各タブ・送信先の検出列を確認。
`×なし` が出た項目があれば、`YAHOO_TRANSFER_CFG.fieldCandidates` または送信先列名（`商品コード` 等）に実ヘッダー名を1つ追記して再確認（Task 1/2のテストは候補追加に影響されない）。

---

### Task 4: 収集・重複判定の純粋ヘルパー（Nodeテスト）

**Files:**
- Modify: `GASアップスクリプト/Project_01/Yahooテンプレ送信.js`
- Modify: `GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js`

**Interfaces:**
- Consumes: `yt_商品コードキー_`, `yt_行から送信値_`
- Produces:
  - `yt_送信計画を作る_(collected, existingCodeKeys: string[]): {toSend, skipDup, skipNoCode}` — `collected` は `{sheet, rowIndex, values}` 配列。商品コード空→skipNoCode、既存/同一実行内重複→skipDup、それ以外→toSend（`{sheet,rowIndex,mapped,codeKey}`）。

- [ ] **Step 1: 失敗するテストを書く**（テストの `process.exit` 直前に追記）

```javascript
// 送信計画: 重複/コード空を除外（collected は mapped 済みオブジェクトを持つ）
const mk = (code, title) => ({
  sheet:'台湾まんが', rowIndex:10,
  mapped: { '商品名':title, '商品コード':code, '発売日':'2026/06/18', 'JANコード':'', '配送グループ管理番号':'佐川' },
});
const collected = [ mk('TWS0001-CM-01','A'), mk('TWS0001-CM-01','A-dup'), mk('','B'), mk('TWS0002-CM-01','C') ];
const plan = ctx.yt_送信計画を作る_(collected, ['TWS0001-CM-01']);
// 1件目は送信先既存で除外、2件目は同一実行重複で除外、3件目はコード空、4件目のみ送信
eq('送信件数', plan.toSend.length, 1);
eq('送信コード', plan.toSend[0].codeKey, 'TWS0002-CM-01');
eq('重複スキップ件数', plan.skipDup, 2);
eq('コード空スキップ件数', plan.skipNoCode, 1);
```

- [ ] **Step 2: テスト実行（失敗確認）**

Run: `node "GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js"`
Expected: FAIL（`yt_送信計画を作る_ is not a function`）

- [ ] **Step 3: 最小実装**

```javascript
function yt_送信計画を作る_(collected, existingCodeKeys) {
  var seen = {};
  (existingCodeKeys || []).forEach(function (k) {
    var key = yt_商品コードキー_(k);
    if (key) seen[key] = true;
  });
  var toSend = [], skipDup = 0, skipNoCode = 0;
  for (var i = 0; i < collected.length; i++) {
    var c = collected[i];
    var mapped = c.mapped;
    var codeKey = yt_商品コードキー_(mapped['商品コード']);
    if (!codeKey) { skipNoCode++; continue; }
    if (seen[codeKey]) { skipDup++; continue; }
    seen[codeKey] = true;
    toSend.push({ sheet: c.sheet, rowIndex: c.rowIndex, mapped: mapped, codeKey: codeKey });
  }
  return { toSend: toSend, skipDup: skipDup, skipNoCode: skipNoCode };
}
```

（`existingCodeKeys` は送信先から読んだ生の商品コード配列でも、キー化済みでも、`yt_商品コードキー_` を通すので両対応。）

- [ ] **Step 4: テスト実行（成功確認）**

Run: `node "GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js"`
Expected: PASS（全 `[OK]`、exit 0）

- [ ] **Step 5: コミット**

```bash
git add "GASアップスクリプト/Project_01/Yahooテンプレ送信.js" "GASアップスクリプト/Project_01/tests/yahoo-transfer.test.js"
git commit -m "feat(yahoo-transfer): 送信計画（重複/コード空スキップ）ヘルパー"
```

---

### Task 5: メイン送信関数（SpreadsheetApp 統合 / 手動検証）

**Files:**
- Modify: `GASアップスクリプト/Project_01/Yahooテンプレ送信.js`

**Interfaces:**
- Consumes: 全 `yt_*` ヘルパー, `YAHOO_TRANSFER_CFG`
- Produces: `台湾_Yahooテンプレへ送信()`（図形ボタン割当用エントリポイント）

- [ ] **Step 1: 実装**

```javascript
function 台湾_Yahooテンプレへ送信() {
  var ss = SpreadsheetApp.getActive();
  var ui = SpreadsheetApp.getUi();
  var cfg = YAHOO_TRANSFER_CFG;

  var lock = LockService.getDocumentLock();
  if (!lock.tryLock(2000)) { ss.toast('他の処理が実行中です。'); return; }
  try {
    // 1) 送信先を開く
    var dss = SpreadsheetApp.openById(cfg.destId);
    var dsh = dss.getSheetByName(cfg.destSheet);
    if (!dsh) { ui.alert('送信先「' + cfg.destSheet + '」が見つかりません。'); return; }
    var dHeaderRows = dsh.getRange(1, 1, cfg.destHeaderRows, dsh.getLastColumn()).getDisplayValues();
    var dMap = yt_ヘッダー名マップ_(dHeaderRows);
    var destCols = {};
    var missing = [];
    Object.keys(cfg.fieldCandidates).forEach(function (f) {
      var c = yt_列を解決_(dMap, [f]);
      if (c < 0) missing.push(f); else destCols[f] = c; // 0始まり
    });
    if (!('商品コード' in destCols) || !('商品名' in destCols)) {
      ui.alert('送信先に必須列が見つかりません（不足: ' + missing.join(', ') + '）。'); return;
    }

    // 2) 送信先の既存商品コードキー
    var dLast = dsh.getLastRow();
    var existingKeys = [];
    if (dLast >= cfg.destDataStartRow) {
      var codeColVals = dsh.getRange(cfg.destDataStartRow, destCols['商品コード'] + 1, dLast - cfg.destDataStartRow + 1, 1).getDisplayValues();
      existingKeys = codeColVals.map(function (r) { return yt_商品コードキー_(r[0]); }).filter(Boolean);
    }

    // 3) 台湾系タブを横断してチェックON行を収集
    var collected = [];
    cfg.srcSheets.forEach(function (name) {
      var sh = ss.getSheetByName(name);
      if (!sh) return;
      var last = sh.getLastRow();
      if (last < 2) return;
      var width = sh.getLastColumn();
      var headerMap = yt_ヘッダー名マップ_(sh.getRange(1, 1, 1, width).getDisplayValues());
      var checks = sh.getRange(2, cfg.checkboxCol, last - 1, 1).getValues();
      for (var i = 0; i < checks.length; i++) {
        if (checks[i][0] !== true) continue;
        var rowIndex = i + 2;
        var values = sh.getRange(rowIndex, 1, 1, width).getDisplayValues()[0];
        collected.push({
          sheet: name, rowIndex: rowIndex,
          mapped: yt_行から送信値_(values, headerMap),
        });
      }
    });

    if (collected.length === 0) { ss.toast('チェックON行がありません。'); return; }

    // 4) 送信計画（重複/コード空スキップ）
    var plan = yt_送信計画を作る_(collected, existingKeys);
    if (plan.toSend.length === 0) {
      ss.toast('送信対象なし（重複' + plan.skipDup + ' / コード無し' + plan.skipNoCode + '）。'); return;
    }

    // 5) 確認ダイアログ
    var preview = plan.toSend.slice(0, 10).map(function (o) {
      return o.codeKey + ' / ' + String(o.mapped['商品名'] || '').slice(0, 30);
    }).join('\n');
    var res = ui.alert('Yahooテンプレへ送信',
      plan.toSend.length + '件を「' + cfg.destSheet + '」の最終行の下へ追記します。\n' +
      '（重複スキップ ' + plan.skipDup + ' / コード無しスキップ ' + plan.skipNoCode + '）\n\n' +
      preview + (plan.toSend.length > 10 ? '\n…ほか' : '') + '\n\n実行する？',
      ui.ButtonSet.YES_NO);
    if (res !== ui.Button.YES) { ui.alert('やめました。'); return; }

    // 6) 追記（送信先の各列へ列ごとに書込）
    var startRow = Math.max(dLast + 1, cfg.destDataStartRow);
    var n = plan.toSend.length;
    Object.keys(destCols).forEach(function (f) {
      var colVals = plan.toSend.map(function (o) { return [o.mapped[f] != null ? o.mapped[f] : '']; });
      dsh.getRange(startRow, destCols[f] + 1, n, 1).setValues(colVals);
    });
    SpreadsheetApp.flush();

    // 7) 送信成功した行のA列チェックをOFF
    var bySheet = {};
    plan.toSend.forEach(function (o) { (bySheet[o.sheet] = bySheet[o.sheet] || []).push(o.rowIndex); });
    Object.keys(bySheet).forEach(function (name) {
      var sh = ss.getSheetByName(name);
      if (!sh) return;
      bySheet[name].forEach(function (rowIndex) {
        sh.getRange(rowIndex, cfg.checkboxCol).setValue(false);
      });
    });

    ss.toast('Yahooへ送信 ' + n + '件 / 重複' + plan.skipDup + ' / コード無し' + plan.skipNoCode);
  } finally {
    lock.releaseLock();
  }
}
```

- [ ] **Step 2: コミット**

```bash
git add "GASアップスクリプト/Project_01/Yahooテンプレ送信.js"
git commit -m "feat(yahoo-transfer): メイン送信関数（収集→重複除外→追記→チェックOFF）"
```

- [ ] **Step 3: 手動検証（ユーザー実行、Task 6のpush後）**

1. 台湾まんがタブで1〜2行だけA列にチェック → `台湾_Yahooテンプレへ送信` を実行（または図形ボタン）
2. 確認ダイアログで件数/プレビューを確認 → 実行
3. ①商品入力シートの最終行の下に正しい列で追記されているか確認
4. ソース側のチェックがOFFになっているか確認
5. もう一度同じ行をチェックして実行 → 「重複スキップ」になるか確認

---

### Task 6: メニュー導線追加 と clasp デプロイ

**Files:**
- Modify: `GASアップスクリプト/Project_01/Onopen_台湾CNなど.js`（onOpen のメニューに項目追加）

**Interfaces:**
- Consumes: `台湾_Yahooテンプレへ送信`, `台湾_Yahoo送信_構造を確認`

- [ ] **Step 1: onOpen メニューに項目追加**

`Onopen_台湾CNなど.js` の `onOpen()` 内、`createMenu('🐈 台湾商品管理')` のチェーンに以下を追加（既存 `.addItem(...)` の並びに合わせて挿入）:

```javascript
    .addSeparator()
    .addItem('📤 Yahooテンプレへ送信（チェック行）', '台湾_Yahooテンプレへ送信')
    .addItem('🔎 Yahoo送信 構造を確認', '台湾_Yahoo送信_構造を確認')
```

（注: 実際の `onOpen` の記述形に合わせること。`addItem` を個別に呼ぶ実装なら同形式で追加。図形ボタンには `台湾_Yahooテンプレへ送信` を割り当てる。メニューは確認・フォールバック用。）

- [ ] **Step 2: コミット**

```bash
git add "GASアップスクリプト/Project_01/Onopen_台湾CNなど.js"
git commit -m "feat(yahoo-transfer): 台湾商品管理メニューに送信/構造確認を追加"
```

- [ ] **Step 3: clasp push（ユーザー認証で実行）**

Run（Project_01 ディレクトリで）:
```bash
cd "GASアップスクリプト/Project_01" && clasp push --force
```
Expected: `Pushed N files`。
（Project_01 はバインドスクリプトでWebアプリではないため、デプロイIDの更新は不要。push のみで反映。シートを開き直すとメニューに項目が出る。）

- [ ] **Step 4: 構造確認 → 本番1〜2件で動作確認**

1. シート再読込 → メニュー「🔎 Yahoo送信 構造を確認」を実行 → 実行ログで全項目が `列N` になっているか確認（`×なし` があれば候補名を追記して再push）
2. Task 5 Step 3 の手動検証を実施
3. 図形ボタンを各台湾系タブに配置し、`台湾_Yahooテンプレへ送信` を割り当て

---

## Self-Review

**1. Spec coverage:**
- 起動=図形ボタン → Task 5（エントリ関数）+ Task 6 Step 4（割当手順）✅
- A列チェック選択 → Task 5 Step 1（checks）✅
- 台湾系タブ横断 → `srcSheets` + Task 5 ✅
- ヘッダー候補名で吸収 → Task 1/2 + Task 5 ✅
- 列マッピング5項目 → `fieldCandidates` + Task 2 ✅
- 重複=商品コードスキップ / コード空スキップ → Task 4 ✅
- 送信先①商品入力シート 2行ヘッダー/3行目〜/最終行下追記 → Task 5 Step 6 ✅
- 確認ダイアログ → Task 5 Step 5 ✅
- 送信後チェックOFF → Task 5 Step 7 ✅
- エラー処理（送信先/タブ欠落・ロック・0件・必須列欠落）→ Task 5 ✅
- 将来拡張（韓国はタブ定数追加）→ `srcSheets` 定数化で対応 ✅

**2. Placeholder scan:** 「実ヘッダー名の最終確定」はTask 3/6の構造確認で実データに対して行う運用ステップであり、コードはランタイム解決で完結（プレースホルダではない）。コードステップは全て実コードを記載。

**3. Type consistency:** `yt_ヘッダー名マップ_` / `yt_列を解決_` / `yt_商品コードキー_` / `yt_行から送信値_` / `yt_送信計画を作る_` の呼び出しと定義が一致。`mapped` は `{[yahooField]: value}`、`destCols` は0始まりindex（書込時 +1）で一貫。
