# 台湾まんが日本語タイトル候補選定 改善 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** MangaUpdates の日本語候補選定で、クエリの中国語エコー（完全一致・部分一致）を除外し、かな入り日本語原題を優先して採用する。

**Architecture:** 変更は `pickPreferredJapaneseCandidate` に集約する。エコー判定を部分一致まで広げ、かな入り候補があればその先頭を返し、無ければ漢字のみ候補を返す。検索・UI・GAS は触らない。

**Tech Stack:** Chrome 拡張 / 素の JavaScript（IIFE） / Node.js による `vm` ユニットテスト

**Spec:** `にゃんたろうず商品登録拡張機能/docs/superpowers/specs/2026-07-16-台湾まんが日本語タイトル候補選定-design.md`

## Global Constraints

- 変更範囲は `core/mangaUpdatesClient.js` の候補選定と `tests/manga-updates-client.test.js` のみ
- 既存回帰（全球高考 / 螢幕情緣 / 今生我来當家主 / K-9）を壊さない
- 中国語を埋めて「解決」にしない（残候補が無ければ空文字）
- かな入りタイトルはエコー除外の対象外

---

## File map

| File | Role |
|---|---|
| `にゃんたろうず商品登録拡張機能/core/mangaUpdatesClient.js` | 候補選定ロジック本体 |
| `にゃんたろうず商品登録拡張機能/tests/manga-updates-client.test.js` | 回帰 + 新規ケース |

---

### Task 1: 失敗するテストを追加（部分一致中国語エコー）

**Files:**
- Modify: `にゃんたろうず商品登録拡張機能/tests/manga-updates-client.test.js`
- Modify: `にゃんたろうず商品登録拡張機能/core/mangaUpdatesClient.js`（`__test` に必要なら `pickPreferredJapaneseCandidate` を公開。既存の `tryResolveMatchedDetail_` 経由でも可）

**Interfaces:**
- Consumes: `titleLookupMangaUpdates.__test.tryResolveMatchedDetail_`, `normalizeTitleKey`
- Produces: 新規回帰ケース（披著狼皮的羊公主 → 狼の皮をかぶった羊姫）

- [ ] **Step 1: 既存テストが通ることを確認**

Run:

```powershell
node "にゃんたろうず商品登録拡張機能/tests/manga-updates-client.test.js"
```

Expected: `manga-updates-client.test.js: ok`

- [ ] **Step 2: 失敗する新規テストを末尾（`console.log` の直前）に追加**

```javascript
// === 部分一致の中国語エコーを除外し、かな入り日本語原題を採用する ===
// クエリ「披著狼皮的羊公主」に対し、Associated 先頭の「披著狼皮的羊」を
// 日本語題として誤採用しないこと。
const sheepJp = '狼の皮をかぶった羊姫';
const sheepDetail = {
  title: "Sheep Princess in Wolf's Clothing",
  associated: [
    { title: '披著狼皮的羊' },
    { title: '披著狼皮的羊公主' },
    { title: "Sheep Princess in Wolf's Clothing" },
    { title: sheepJp },
  ],
};
const sheepRow = {
  hit_title: '披著狼皮的羊公主',
  record: {
    title: "Sheep Princess in Wolf's Clothing",
    associated: [{ title: '披著狼皮的羊公主' }],
  },
};
const sheepQueries = ['披著狼皮的羊公主'];
const sheepKeys = sheepQueries.map(t.normalizeTitleKey);
const sheepResolved = t.tryResolveMatchedDetail_(
  sheepDetail,
  sheepRow,
  sheepKeys,
  sheepQueries,
  []
);
if (sheepResolved.japaneseTitle !== sheepJp) {
  throw new Error(
    `partial Chinese echo should yield JP title ${sheepJp}, got: ${sheepResolved.japaneseTitle}`
  );
}
```

- [ ] **Step 3: テストを実行し、失敗することを確認**

Run:

```powershell
node "にゃんたろうず商品登録拡張機能/tests/manga-updates-client.test.js"
```

Expected: FAIL（`partial Chinese echo should yield JP title ... got: 披著狼皮的羊` など）

- [ ] **Step 4: Commit（テストのみ）**

```powershell
git add "にゃんたろうず商品登録拡張機能/tests/manga-updates-client.test.js"
git commit -m @"
test: 部分一致中国語エコーの誤採用を再現する回帰を追加
"@
```

---

### Task 2: `pickPreferredJapaneseCandidate` を仕様どおりに実装

**Files:**
- Modify: `にゃんたろうず商品登録拡張機能/core/mangaUpdatesClient.js`（`pickPreferredJapaneseCandidate` 付近、おおよそ L118–157）

**Interfaces:**
- Consumes: `normalizeTitleKey`, `normalizeCjkEchoKey`, `hasKanaJapaneseSignal`
- Produces: 更新された `pickPreferredJapaneseCandidate(candidates, echoQueries) -> string`

- [ ] **Step 1: エコー判定ヘルパーを追加（`pickPreferredJapaneseCandidate` の直前）**

```javascript
  function isPartialEchoKey(candidateKey, queryKey) {
    if (!candidateKey || !queryKey) return false;
    var shorter = candidateKey.length <= queryKey.length ? candidateKey : queryKey;
    var longer = candidateKey.length <= queryKey.length ? queryKey : candidateKey;
    if (shorter.length < 4) return false;
    return longer.indexOf(shorter) >= 0;
  }

  function isChineseEchoOfQueries(candidate, echoQueries) {
    // かな入りは日本語原題の可能性が高いのでエコー除外しない
    if (hasKanaJapaneseSignal(candidate)) return false;
    var qs = Array.isArray(echoQueries) ? echoQueries : (echoQueries ? [echoQueries] : []);
    var key = normalizeTitleKey(candidate);
    var variantKey = normalizeCjkEchoKey(candidate);
    var i;
    for (i = 0; i < qs.length; i += 1) {
      var qk = normalizeTitleKey(qs[i]);
      var qvk = normalizeCjkEchoKey(qs[i]);
      if (key && qk && (key === qk || isPartialEchoKey(key, qk))) return true;
      if (variantKey && qvk && (variantKey === qvk || isPartialEchoKey(variantKey, qvk))) return true;
    }
    return false;
  }
```

- [ ] **Step 2: `pickPreferredJapaneseCandidate` を置き換え**

```javascript
  function pickPreferredJapaneseCandidate(candidates, echoQueries) {
    var list = candidates || [];
    if (!list.length) return '';

    var pool = [];
    var i;
    for (i = 0; i < list.length; i += 1) {
      if (isChineseEchoOfQueries(list[i], echoQueries)) continue;
      pool.push(list[i]);
    }
    // 全部エコーなら空（中国語を無理に採用しない）
    if (!pool.length) return '';

    var kanaPool = [];
    for (i = 0; i < pool.length; i += 1) {
      if (hasKanaJapaneseSignal(pool[i])) kanaPool.push(pool[i]);
    }
    if (kanaPool.length) {
      return String(kanaPool[0] || '').trim();
    }
    // かな無し（K-9 等）は Associated 表示順の先頭を採用
    return String(pool[0] || '').trim();
  }
```

重要: 旧実装の `if (!pool.length) pool = list;`（全部エコーなら元リストに戻す）は **削除**する。これが中国語誤採用の逃げ道になる。

- [ ] **Step 3: 全テスト実行**

Run:

```powershell
node "にゃんたろうず商品登録拡張機能/tests/manga-updates-client.test.js"
```

Expected: `manga-updates-client.test.js: ok`

- [ ] **Step 4: Commit**

```powershell
git add "にゃんたろうず商品登録拡張機能/core/mangaUpdatesClient.js"
git commit -m @"
fix: 日本語候補選定で中国語部分エコーを除外し、かな入り原題を優先
"@
```

---

### Task 3: 互換ケースの明示テスト補強（任意だが推奨）

**Files:**
- Modify: `にゃんたろうず商品登録拡張機能/tests/manga-updates-client.test.js`

既存ファイルに全球高考 / 螢幕情緣 / 今生我来當家主 / K-9 は既にある。Task 2 後にそれらが通っていれば本 Task はスキップ可。

- [ ] **Step 1: Task 2 後のテスト結果を再確認**

Run:

```powershell
node "にゃんたろうず商品登録拡張機能/tests/manga-updates-client.test.js"
```

Expected: `manga-updates-client.test.js: ok`（既存4系統 + 新規 sheep ケース）

- [ ] **Step 2: 追加コミットが不要ならスキップ。失敗した既存ケースがあれば最小修正して再コミット**

---

## Spec coverage checklist

| Spec 要件 | Task |
|---|---|
| 部分一致中国語エコー除外 | Task 1–2 |
| かな入り優先 | Task 2 |
| かな無し漢字題（K-9）維持 | Task 2（kanaPool が空なら pool[0]） |
| 残候補なし → 空文字 | Task 2（`if (!pool.length) return ''`） |
| 既存互換ケース | Task 1 Step1 / Task 2 Step3 / Task 3 |
| 検索・UI・GAS 非変更 | 全 Task（触らない） |

## Out of scope（次フェーズ候補）

より精度の高い日本語タイトル取得の別手段は、実装プラン外。必要なら別スペックで検討する。
