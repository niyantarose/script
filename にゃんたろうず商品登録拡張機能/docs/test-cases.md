# test cases

- Aladin manga ページで popup が site / genre / folder preview を表示する
- manual save で queue が 1 件ずつ進む
- reRunPolicy=continue で連番が継続する
- reRunPolicy=reset で bucket ごとに 1 から再開する
- saveExtractedJson を OFF にすると extracted.json を作らない
